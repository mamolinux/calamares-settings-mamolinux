# The .desktop file uses bash to run firefox as the local
# user. bash is included in the system.
calamares-settings-mamolinux: desktop-command-not-in-package usr/share/applications/mamolinux-calamares.desktop bash
